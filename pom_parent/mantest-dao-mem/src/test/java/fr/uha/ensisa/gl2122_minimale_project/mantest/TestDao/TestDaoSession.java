package fr.uha.ensisa.gl2122_minimale_project.mantest.TestDao;


import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import fr.uha.ensisa.gl.gl2122_minimale_project.mantest_dao.Dao_mem.DaoSession;



public class TestDaoSession {
	/*
	private DaoSession sut;
	
	@BeforeEach
	private void createModel() {
		this.sut = new DaoSession();
	}
	
	@Test
	public void testConstruction() {
		assertEquals("now", this.sut.getDate());
		assertEquals(0, this.sut.getStatus());
		assertEquals(-1, this.sut.getFailed());
		assertEquals(null, this.sut.getCommentary());
	}
	
	
	@Test
	public void testFailedTrue() {
		int fail = 5;
		this.sut.setFailed(fail);
		assertEquals(fail, this.sut.getFailed());
		assertEquals(-1, this.sut.getStatus());
	}
	
	@Test
	public void testFailedFalse() {
		int fail = -1;
		this.sut.setFailed(fail);
		assertEquals(fail, this.sut.getFailed());
		assertEquals(1, this.sut.getStatus());
	}

	@Test
	public void testCommentary() {
		String comment = "test";
		int fail = 5;
		this.sut.setCommentary(comment);
		assertEquals(null, this.sut.getCommentary());
		
		this.sut.setFailed(fail);
		this.sut.setCommentary(comment);
		assertEquals(comment, this.sut.getCommentary());
	}
		*/
}
